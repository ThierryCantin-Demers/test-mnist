[package]
name = "test-mnist"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = {
    path = "../burn/crates/burn",
    features = ["std", "tui", "train", "vision", "flex", "wgpu"],
    default-features = false,
}
